import { frame } from '../../../data/blocks/functions';
import SpriteObject from '../sprite_object';

class Key extends SpriteObject {
  constructor(data){
    let { spriteSheet, objData } = data;
    super(spriteSheet, objData);
  }



}
export default Key;
